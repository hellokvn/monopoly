{
  "name": "@monopoly/sdk",
  "version": "1.0.0",
  "description": "SDK",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/dashify": "^1.0.1"
  },
  "dependencies": {
    "dashify": "^2.0.0"
  }
}
