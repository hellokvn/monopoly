{
  "name": "@monopoly/sdk",
  "version": "1.0.0",
  "description": "SDK",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ts-proto": "^1.147.2"
  },
  "dependencies": {
    "random-int": "^3.0.0"
  }
}
